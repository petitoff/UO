for gap = floor(n / 2) to gap > 0
    for i = gap to i < n
        curr = arr[i]

        for j = i to j >= gap && arr[j-gap] > curr
            arr[j] = arr[j-gap]
            j = j - gap
        
        arr[j] = curr
        i = i + 1
    
    gap = floor(n / 2)
